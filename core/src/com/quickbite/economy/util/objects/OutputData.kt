package com.quickbite.economy.util.objects

/**
 * A class to hold data for output for inventory items (usually)
 * @param exportable If the output is exportable
 */
data class OutputData(var exportable:Boolean = true)